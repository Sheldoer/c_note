#include<stdio.h>
int main(void)
{
	/*int a[1000]={0};
	printf("请输入总人数：");
	int n;
	scanf("%d",&n);
	for(int i=0,cnt=0,sum=0;sum!=n-1;++i)                      //习题三
	{
		if(a[i%n]==3)
			continue;
		if((a[i%n]=(cnt++)%3+1)==3)
			sum++;
	}
	for(i=0;i<n;++i)
		if(a[i]!=3)
			break;
	printf("最后留下的是原来第%d个人\n",i+1);
	*/
	
	return 0;
}